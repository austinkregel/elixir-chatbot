# Dependencies for the Ouro inference sidecar (scripts/ouro_server.py)
# Install in a venv:
#   python3 -m venv .venv-ouro
#   source .venv-ouro/bin/activate
#   pip install -r scripts/requirements-ouro.txt
torch>=2.4.0
transformers>=4.54.0,<4.56.0
accelerate>=0.30.0
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
psutil>=5.9.0
